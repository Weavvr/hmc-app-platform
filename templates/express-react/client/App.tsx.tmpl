/**
 * {{appName}} - Root App Component
 *
 * Generated by HMC CLI. Add your routes and layout here.
 */

import { Routes, Route, Link } from "react-router-dom";

function HomePage() {
  return (
    <div className="flex flex-col items-center justify-center min-h-[60vh] gap-4">
      <h1 className="text-4xl font-bold">{{appName}}</h1>
      <p className="text-lg text-muted-foreground">{{description}}</p>
      <p className="text-sm text-muted-foreground">
        Edit <code className="bg-muted px-1 rounded">client/App.tsx</code> to get started.
      </p>
    </div>
  );
}

function NotFoundPage() {
  return (
    <div className="flex flex-col items-center justify-center min-h-[60vh] gap-4">
      <h1 className="text-4xl font-bold">404</h1>
      <p className="text-lg text-muted-foreground">Page not found.</p>
      <Link to="/" className="text-primary underline">
        Go home
      </Link>
    </div>
  );
}

export default function App() {
  return (
    <div className="min-h-screen bg-background text-foreground">
      {/* Navigation */}
      <header className="border-b">
        <nav className="container mx-auto flex h-14 items-center px-4">
          <Link to="/" className="font-semibold text-lg">
            {{appName}}
          </Link>
          <div className="ml-auto flex items-center gap-4">
            {/* Add navigation links here */}
          </div>
        </nav>
      </header>

      {/* Main Content */}
      <main className="container mx-auto px-4 py-8">
        <Routes>
          <Route path="/" element={<HomePage />} />
          {/* Add your routes here */}
          <Route path="*" element={<NotFoundPage />} />
        </Routes>
      </main>
    </div>
  );
}
